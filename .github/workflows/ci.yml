name: CI/CD

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      checks: write
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Grant execute permission to Gradle
        run: chmod +x gradlew

      # ── Build
      - name: Build
        run: ./gradlew build -x test

      # ── Tests
      - name: Run tests
        run: ./gradlew test

      # ── Tests d'intégration
      - name: Run integration tests
        run: ./gradlew testIntegration

      # ── Tests de composants
      - name: Run component tests
        run: ./gradlew testComponent

      # ── Rapport de couverture JaCoCo (unit + intégration)
      - name: Generate JaCoCo report
        run: ./gradlew jacocoTestReport

      - name: Publish test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: |
            build/test-results/test/*.xml
            build/test-results/testIntegration/*.xml
            build/test-results/testComponent/*.xml

      # ── Publier la couverture dans le pipeline
      - name: Publish coverage report
        uses: madrapps/jacoco-report@v1.6.1
        with:
          paths: build/reports/jacoco/test/jacocoTestReport.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 80
          min-coverage-changed-files: 80
          title: Code Coverage

      # ── Analyse statique Detekt
      - name: Run Detekt
        run: ./gradlew detekt

      # ── Tests d'architecture
      - name: Run architecture tests
        run: ./gradlew testArchitecture

      # ── Tests de mutation PI Test
      - name: Run mutation tests (PIT)
        run: ./gradlew pitest

      - name: Publish mutation test report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pitest-report
          path: build/reports/pitest/
